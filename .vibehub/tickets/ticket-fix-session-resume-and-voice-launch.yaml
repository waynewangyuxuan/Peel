{
  "acceptance": [
    {
      "acceptance_id": "resume-before-turn",
      "criterion": "The App Server client distinguishes read-only thread/read from a loaded session and guarantees thread/resume before the first turn/start for a stored Thread; new and forked Threads do not pay an unnecessary resume, reconnect or thread/closed invalidates loaded state, and concurrent sends cannot issue duplicate resume requests."
    },
    {
      "acceptance_id": "session-recovery-regression",
      "criterion": "An automated App Server fixture that rejects turn/start for an unreadied stored Thread reproduces the dogfood failure and then proves Peel resumes exactly once, starts exactly one Turn, continues receiving events, and can send again after a simulated reconnect/unload without duplicating a user Turn."
    },
    {
      "acceptance_id": "send-failure-draft-safety",
      "criterion": "If resume or turn/start still fails because Codex truly cannot recover the Thread, Focus shows a concise actionable Composer error instead of an Electron `Error invoking remote method`/raw RPC message; the exact text and attachments remain editable and a retry remains possible."
    },
    {
      "acceptance_id": "independent-voice-launch",
      "criterion": "The production package contains the microphone and Speech Recognition usage declarations and native helper, and a packaged Peel opened independently through macOS LaunchServices completes one real Apple Speech transcription of a known spoken phrase into the current editable Thread Draft. Existing Draft text remains present, no turn/start is emitted, and captured macOS evidence attributes the privacy request to com.peel.desktop rather than Codex/ChatGPT; a fake helper or text insertion cannot satisfy this criterion."
    },
    {
      "acceptance_id": "voice-failure-clarity",
      "criterion": "The native Voice boundary records both exit code and termination signal; privacy termination, permission denial, capture interruption, timeout, and recognition failure produce specific recoverable Composer messages, never auto-send, and never erase or replace the existing per-Thread Draft."
    },
    {
      "acceptance_id": "integrated-verification",
      "criterion": "Repository tests and build pass, the packaged-app verification exercises independent-launch prerequisites, and a production Electron journey retains Picker, Focus, Markdown, Overview, Fork, Voice editing/no-auto-send, Approval, Diff, Draft persistence, and restart behavior."
    }
  ],
  "constraints": [
    "Codex remains the Source of Truth for Thread and Transcript; do not copy rollout history into Peel or rebind a missing Thread by fuzzy title.",
    "A failed send must never clear the Composer Draft or attachments, and an automatic retry is allowed only when the failed RPC cannot have created a Turn.",
    "Voice remains real Apple Speech Recognition and editable Draft insertion; do not replace it with fake text, auto-send, or defer it.",
    "Do not request or mutate privacy permission for ChatGPT/Codex. The supported product path is an independently launched Peel bundle with its own declared identity.",
    "Preserve the current Demo-aligned Focus, Markdown, and Overview presentation while changing lifecycle and error states."
  ],
  "context": "Direct dogfood exposed two concrete lifecycle defects. Peel reads stored Threads with thread/read and then calls turn/start without first loading them through thread/resume, so Codex correctly returns `thread not found` even though the rollout files remain present and a fresh App Server can read them. Separately, the dogfood instance was launched as a child of Codex/ChatGPT; macOS attributed Speech Recognition to com.openai.codex, refused it for the missing usage description, and terminated peel-speech, while the independently launched packaged Peel is attributed to com.peel.desktop. This focused repair restores both daily-use paths and makes the remaining failure states understandable and non-destructive.",
  "context_refs": [
    {
      "purpose": "Places resume/reconnect/session ownership in the App Server adapter rather than ad hoc renderer retries.",
      "ref": ".vibehub/rooms/product/decision-app-server-adapter-architecture.yaml"
    },
    {
      "purpose": "Requires recovery from authoritative Codex history without creating a shadow Transcript.",
      "ref": ".vibehub/rooms/product/contract-source-of-truth-ownership.yaml"
    },
    {
      "purpose": "Keeps Voice real, editable, non-sending, and Draft-safe across failures.",
      "ref": ".vibehub/rooms/product/contract-voice-dictation-draft.yaml"
    },
    {
      "purpose": "Requires usable errors and preserved per-Thread Composer state in the first-class Chat flow.",
      "ref": ".vibehub/rooms/product/contract-focus-chat-fidelity.yaml"
    },
    {
      "purpose": "Defines Voice and zero Draft loss as v0.1 release gates.",
      "ref": ".vibehub/rooms/product/contract-v01-core-spatial-fork-loop.yaml"
    }
  ],
  "kind": "ticket",
  "maturity": "firm",
  "outcome": "Peel reliably continues stored Codex Chats and performs recoverable Voice Dictation from the independently launched packaged app, without raw IPC/server failures or Draft loss.",
  "provenance_refs": [
    "conversation:2026-08-27-send-and-voice-dogfood-failure",
    "https://learn.chatgpt.com/docs/app-server",
    "diagnostic:macos-tcc-2026-08-27T00:25:49-0700",
    "commit:44115239ad634a3c39ddf912e24d4fafd80bcb04"
  ],
  "relations": [
    {
      "rationale": "This lifecycle repair must preserve the exact current integrated Focus, Markdown, and Overview baseline.",
      "target_ticket_id": "ticket-fix-chat-markdown-presentation",
      "type": "depends_on"
    }
  ],
  "schema_version": 1,
  "ticket_id": "ticket-fix-session-resume-and-voice-launch"
}
